---
phase: 01-infrastructure-and-auth-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - "01-03"
files_modified:
  - backend/src/test/java/com/opspilot/config/TestcontainersConfiguration.java
  - backend/src/test/java/com/opspilot/security/JwtServiceTest.java
  - backend/src/test/java/com/opspilot/controller/AuthControllerIntegrationTest.java
  - backend/src/test/resources/application-test.properties
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-06
  - INFRA-02

must_haves:
  truths:
    - "JwtServiceTest passes: token generation, username extraction, valid-token check, expired-token check"
    - "AuthControllerIntegrationTest passes: login success (200+token), login failure (401), /me with token (200+profile), /me without token (401)"
    - "Flyway applies V1 and V2 migrations inside the Testcontainers PostgreSQL container"
    - "Seeded admin@example.com login succeeds in the integration test"
    - "All tests pass with ./mvnw test"
  artifacts:
    - path: "backend/src/test/java/com/opspilot/config/TestcontainersConfiguration.java"
      provides: "@ServiceConnection PostgreSQL container shared across integration tests"
      contains: "@TestConfiguration, PostgreSQLContainer, @ServiceConnection"
    - path: "backend/src/test/java/com/opspilot/security/JwtServiceTest.java"
      provides: "Unit tests for JwtService"
      contains: "generateToken, extractUsername, isTokenValid, expired token"
    - path: "backend/src/test/java/com/opspilot/controller/AuthControllerIntegrationTest.java"
      provides: "Integration tests for POST /api/auth/login and GET /api/auth/me"
      contains: "@SpringBootTest, @Import(TestcontainersConfiguration.class)"
    - path: "backend/src/test/resources/application-test.properties"
      provides: "Test profile overrides"
      contains: "jwt.secret, spring.flyway.clean-disabled=false"
  key_links:
    - from: "AuthControllerIntegrationTest.java"
      to: "TestcontainersConfiguration.java"
      via: "@Import annotation"
      pattern: "@Import\\(TestcontainersConfiguration"
    - from: "TestcontainersConfiguration.java"
      to: "PostgreSQLContainer"
      via: "@ServiceConnection — auto-configures datasource URL/credentials"
      pattern: "@ServiceConnection"
---

<objective>
Write and verify all tests for Phase 1: a shared Testcontainers configuration, JwtService unit tests, and AuthController integration tests. Run the full test suite to confirm the phase succeeds.

Purpose: Provides automated proof that AUTH-01 through AUTH-06 are working end-to-end. Integration tests use a real PostgreSQL container with real Flyway migrations — the same code path as production.

Output:
- TestcontainersConfiguration.java — shared @TestConfiguration with PostgreSQLContainer
- JwtServiceTest.java — unit tests for token generation, extraction, validation, expiry
- AuthControllerIntegrationTest.java — integration tests for /login and /me endpoints
- application-test.properties — test-specific overrides (already created in plan 01-01; verify/update here)
</objective>

<execution_context>
@/home/gianluca/applicazioni/VibeCoding/git/opspilot-ai-desk/.claude/get-shit-done/workflows/execute-plan.md
@/home/gianluca/applicazioni/VibeCoding/git/opspilot-ai-desk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/gianluca/applicazioni/VibeCoding/git/opspilot-ai-desk/.planning/phases/01-infrastructure-and-auth-foundation/01-CONTEXT.md
@/home/gianluca/applicazioni/VibeCoding/git/opspilot-ai-desk/.planning/phases/01-infrastructure-and-auth-foundation/01-RESEARCH.md
@/home/gianluca/applicazioni/VibeCoding/git/opspilot-ai-desk/.planning/phases/01-infrastructure-and-auth-foundation/01-03-SUMMARY.md

<interfaces>
<!-- Key contracts for test code. Read source files before writing tests. -->

AuthResponse record: AuthResponse(String token, String email, String role)
UserResponse record: UserResponse(Long id, String email, String role, String fullName)
LoginRequest record: LoginRequest(String email, String password)

POST /api/auth/login
  - Body: { "email": "...", "password": "..." }
  - 200 OK: { "token": "<jwt>", "email": "...", "role": "..." }
  - 401: { "error": "Invalid credentials" }

GET /api/auth/me
  - Header: Authorization: Bearer <token>
  - 200 OK: { "id": 1, "email": "...", "role": "...", "fullName": "..." }
  - 401 when no/invalid token

Seeded credentials (from V2 migration):
  - admin@example.com / admin123 / ADMIN
  - operator@example.com / operator123 / OPERATOR

JwtService methods available for unit test:
  - generateToken(UserDetails)
  - extractUsername(String token)
  - isTokenValid(String token, UserDetails userDetails)
  - (expiry tested by creating a token and fast-forwarding or using a short-lived secret)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared Testcontainers config and application-test.properties</name>

  <read_first>
    - .planning/phases/01-infrastructure-and-auth-foundation/01-RESEARCH.md — TestcontainersConfiguration code example, Validation Architecture section
    - backend/src/test/resources/application-test.properties (if it already exists from plan 01-01)
  </read_first>

  <action>
FILE: backend/src/test/java/com/opspilot/config/TestcontainersConfiguration.java

- package com.opspilot.config
- @TestConfiguration(proxyBeanMethods = false)
- public class TestcontainersConfiguration
- One @Bean @ServiceConnection static method:
  static PostgreSQLContainer<?> postgresContainer() {
    return new PostgreSQLContainer<>("postgres:16-alpine");
  }
- Imports: org.springframework.boot.test.context.TestConfiguration,
           org.springframework.boot.testcontainers.service.connection.ServiceConnection,
           org.testcontainers.containers.PostgreSQLContainer,
           org.springframework.context.annotation.Bean
- The @ServiceConnection annotation auto-configures spring.datasource.url/username/password
  from the running container — no manual property overrides needed for datasource in tests

FILE: backend/src/test/resources/application-test.properties

If this file already exists (created in plan 01-01), verify it contains all of the following. If missing, create it:
- spring.flyway.clean-disabled=false         (allow clean in tests for fresh DB state)
- spring.jpa.show-sql=false
- jwt.secret=dGVzdC1zZWNyZXQta2V5LWZvci1qdW5pdC10ZXN0cy1vbmx5LXVzZS0xMjhjaGFycw==
  (base64-encoded test secret; safe to commit — tests only)
- jwt.expiration-ms=3600000
- logging.level.com.opspilot=INFO
- spring.datasource.url=  (leave empty; @ServiceConnection overrides it at test runtime)
  </action>

  <behavior>
    - TestcontainersConfiguration provides a static PostgreSQLContainer with @ServiceConnection
    - When imported via @Import, it auto-wires datasource URL/credentials for the test application context
    - application-test.properties provides jwt.secret so JwtService can generate tokens without real env vars
  </behavior>

  <verify>
    <automated>test -f /home/gianluca/applicazioni/VibeCoding/git/opspilot-ai-desk/backend/src/test/java/com/opspilot/config/TestcontainersConfiguration.java && grep -c "ServiceConnection" /home/gianluca/applicazioni/VibeCoding/git/opspilot-ai-desk/backend/src/test/java/com/opspilot/config/TestcontainersConfiguration.java</automated>
  </verify>

  <done>
    - TestcontainersConfiguration.java exists with @TestConfiguration and @ServiceConnection PostgreSQLContainer
    - application-test.properties contains jwt.secret and spring.flyway.clean-disabled=false
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: JwtService unit tests</name>

  <read_first>
    - .planning/phases/01-infrastructure-and-auth-foundation/01-RESEARCH.md — Validation Architecture → Phase Requirements → Test Map (AUTH-03, AUTH-04)
    - backend/src/main/java/com/opspilot/security/JwtService.java (read the actual implementation)
  </read_first>

  <behavior>
    - generateToken(userDetails) returns a non-blank string
    - extractUsername(token) returns the email used when generating
    - isTokenValid(token, userDetails) returns true for a fresh token with matching username
    - isTokenValid returns false when token's username doesn't match userDetails
    - isTokenValid returns false when token is expired (use reflection or a short secret to simulate)
  </behavior>

  <action>
FILE: backend/src/test/java/com/opspilot/security/JwtServiceTest.java

- package com.opspilot.security
- @ExtendWith(SpringExtension.class)  (loads Spring context for @Value injection)
- @SpringBootTest(classes = {JwtService.class})  — narrow context, no web layer
- @ActiveProfiles("test")  — loads application-test.properties
- @Autowired JwtService jwtService
- Helper: private UserDetails userDetails(String email) {
    return org.springframework.security.core.userdetails.User.builder()
      .username(email).password("ignored").roles("OPERATOR").build();
  }

Tests:
1. generateToken_returnsNonBlankToken():
   var token = jwtService.generateToken(userDetails("test@example.com"));
   assertThat(token).isNotBlank();

2. extractUsername_returnsEmail():
   var token = jwtService.generateToken(userDetails("user@test.com"));
   assertThat(jwtService.extractUsername(token)).isEqualTo("user@test.com");

3. isTokenValid_withMatchingUser_returnsTrue():
   var ud = userDetails("valid@test.com");
   var token = jwtService.generateToken(ud);
   assertThat(jwtService.isTokenValid(token, ud)).isTrue();

4. isTokenValid_withDifferentUser_returnsFalse():
   var token = jwtService.generateToken(userDetails("a@test.com"));
   assertThat(jwtService.isTokenValid(token, userDetails("b@test.com"))).isFalse();

5. isTokenValid_withExpiredToken_returnsFalse():
   Use ReflectionTestUtils.setField(jwtService, "expirationMs", -1000L) to set negative expiry,
   generate a token (immediately expired), then assert isTokenValid returns false.
   Restore field after test with @AfterEach or use a try-finally block.
   Alternatively: catch the JwtException that parseSignedClaims throws for expired tokens and assert that isTokenValid returns false.

Use assertThat from AssertJ (bundled in spring-boot-starter-test).
Do NOT mock JwtService itself — this is a unit test of the real implementation.
  </action>

  <verify>
    <automated>cd /home/gianluca/applicazioni/VibeCoding/git/opspilot-ai-desk/backend && ./mvnw test -Dtest=JwtServiceTest -q 2>&1 | tail -10</automated>
  </verify>

  <done>
    - JwtServiceTest has at least 5 test methods
    - All 5 tests pass: ./mvnw test -Dtest=JwtServiceTest shows BUILD SUCCESS
    - No test uses mocks for JwtService — tests the real implementation
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AuthController integration tests and full test suite run</name>

  <read_first>
    - .planning/phases/01-infrastructure-and-auth-foundation/01-RESEARCH.md — Integration Test Pattern for Auth Endpoints (full code example)
    - backend/src/main/java/com/opspilot/dto/response/AuthResponse.java
    - backend/src/main/java/com/opspilot/dto/response/UserResponse.java
  </read_first>

  <behavior>
    - POST /api/auth/login with valid seeded credentials returns 200 and non-blank token (AUTH-01, AUTH-06)
    - POST /api/auth/login with wrong password returns 401 (AUTH-01)
    - GET /api/auth/me with valid Bearer token returns 200 with email and role (AUTH-02)
    - GET /api/auth/me without token returns 401 (AUTH-03)
    - Flyway migrations V1 + V2 run successfully inside Testcontainers (INFRA-02)
  </behavior>

  <action>
FILE: backend/src/test/java/com/opspilot/controller/AuthControllerIntegrationTest.java

- package com.opspilot.controller
- @SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)
- @Import(TestcontainersConfiguration.class)  — pulls in Testcontainers PostgreSQL with @ServiceConnection
- @ActiveProfiles("test")  — loads application-test.properties (jwt.secret for token generation)
- @Autowired TestRestTemplate restTemplate

Tests (use the exact test method names so Surefire can run them individually):

1. login_withValidCredentials_returnsToken():
   var request = new LoginRequest("admin@example.com", "admin123");
   var response = restTemplate.postForEntity("/api/auth/login", request, AuthResponse.class);
   assertThat(response.getStatusCode()).isEqualTo(HttpStatus.OK);
   assertThat(response.getBody()).isNotNull();
   assertThat(response.getBody().token()).isNotBlank();
   assertThat(response.getBody().email()).isEqualTo("admin@example.com");
   assertThat(response.getBody().role()).isEqualTo("ADMIN");

2. login_withInvalidCredentials_returns401():
   var request = new LoginRequest("admin@example.com", "wrongpassword");
   var response = restTemplate.postForEntity("/api/auth/login", request, String.class);
   assertThat(response.getStatusCode()).isEqualTo(HttpStatus.UNAUTHORIZED);

3. login_withOperatorCredentials_returnsToken():
   var request = new LoginRequest("operator@example.com", "operator123");
   var response = restTemplate.postForEntity("/api/auth/login", request, AuthResponse.class);
   assertThat(response.getStatusCode()).isEqualTo(HttpStatus.OK);
   assertThat(response.getBody().role()).isEqualTo("OPERATOR");

4. me_withValidToken_returnsUserProfile():
   // First login
   var loginResp = restTemplate.postForEntity("/api/auth/login",
     new LoginRequest("operator@example.com", "operator123"), AuthResponse.class);
   String token = loginResp.getBody().token();
   // Then call /me
   var headers = new HttpHeaders();
   headers.setBearerAuth(token);
   var response = restTemplate.exchange("/api/auth/me", HttpMethod.GET,
     new HttpEntity<>(headers), UserResponse.class);
   assertThat(response.getStatusCode()).isEqualTo(HttpStatus.OK);
   assertThat(response.getBody().email()).isEqualTo("operator@example.com");
   assertThat(response.getBody().role()).isEqualTo("OPERATOR");

5. me_withoutToken_returns401():
   var response = restTemplate.getForEntity("/api/auth/me", String.class);
   assertThat(response.getStatusCode()).isEqualTo(HttpStatus.UNAUTHORIZED);

6. login_withEmptyEmail_returns400():
   var request = new LoginRequest("", "password");
   var response = restTemplate.postForEntity("/api/auth/login", request, String.class);
   assertThat(response.getStatusCode()).isEqualTo(HttpStatus.BAD_REQUEST);

After writing and verifying the integration tests pass individually, run the full test suite:
  cd backend && ./mvnw test

All tests must pass. If any fail, read the Surefire failure output and fix the root cause before marking this task done.

NOTE on $2b$ BCrypt prefix (Assumption A1 from RESEARCH.md): If tests 1 or 3 return 401 despite the correct password being used, the BCrypt hash prefix in V2__seed_users.sql may be incompatible. In that case:
1. Do NOT edit V2__seed_users.sql — Flyway checksums are immutable
2. Write a one-off test to print fresh $2a$-prefixed hashes: new BCryptPasswordEncoder(12).encode("admin123")
3. Create backend/src/main/resources/db/migration/V3__fix_seed_hashes.sql with UPDATE statements
4. Re-run the test suite
  </action>

  <verify>
    <automated>cd /home/gianluca/applicazioni/VibeCoding/git/opspilot-ai-desk/backend && ./mvnw test -q 2>&1 | tail -15</automated>
  </verify>

  <done>
    - AuthControllerIntegrationTest has 6 test methods
    - All tests pass: ./mvnw test shows BUILD SUCCESS with 0 failures
    - Surefire report shows tests run > 0 for both JwtServiceTest and AuthControllerIntegrationTest
    - No test failures or errors in the output
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test context → Testcontainers | Tests spin up a real PostgreSQL container; no shared state between runs |
| application-test.properties → JVM | Test JWT secret is committed (intentionally safe — tests only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | application-test.properties jwt.secret | accept | Test secret is intentionally committed; it is base64 of a known string. Tests use RANDOM_PORT — no real exposure |
| T-04-02 | Tampering | Testcontainers database | accept | Each test run gets a fresh container; Flyway clean-disabled=false for test profile allows clean-up |
</threat_model>

<verification>
Phase 1 overall success check:

```bash
# 1. Start PostgreSQL
cd /home/gianluca/applicazioni/VibeCoding/git/opspilot-ai-desk && make up

# 2. Verify Flyway runs on startup (copy .env.example to backend/.env and set JWT_SECRET first)
cp .env.example backend/.env
# Edit backend/.env: set JWT_SECRET to a real base64 value:
# JWT_SECRET=$(openssl rand -base64 64 | tr -d '\n')

# 3. Start backend
make backend &
# Wait for "Started OpsPilotApplication" in logs

# 4. Smoke test login
curl -s -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@example.com","password":"admin123"}' | jq .
# Expect: {"token":"ey...","email":"admin@example.com","role":"ADMIN"}

# 5. Smoke test /me
TOKEN=$(curl -s -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@example.com","password":"admin123"}' | jq -r .token)
curl -s http://localhost:8080/api/auth/me -H "Authorization: Bearer $TOKEN" | jq .
# Expect: {"id":1,"email":"admin@example.com","role":"ADMIN","fullName":"Admin User"}

# 6. Smoke test 401 without token
curl -s -o /dev/null -w "%{http_code}" http://localhost:8080/api/auth/me
# Expect: 401

# 7. Run full test suite
make test
# Expect: BUILD SUCCESS, 0 failures
```
</verification>

<success_criteria>
- ./mvnw test passes with BUILD SUCCESS and 0 failures
- All 5 integration test scenarios pass (login success, login failure, /me with token, /me without token, operator login)
- All 5 JwtService unit tests pass
- Testcontainers starts a real PostgreSQL and Flyway applies V1 + V2 migrations inside it
- Phase 1 success criteria from ROADMAP.md are all satisfied:
  1. make up starts PostgreSQL; make backend starts the backend
  2. POST /login with admin credentials returns JWT
  3. GET /me with valid JWT returns email and role
  4. Protected endpoint without JWT returns 401
  5. CORS configured for localhost:4200
</success_criteria>

<output>
After completion, create .planning/phases/01-infrastructure-and-auth-foundation/01-04-SUMMARY.md
</output>
